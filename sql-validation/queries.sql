-- View all users
SELECT * FROM users;

-- Find user by ID
SELECT * FROM users WHERE id = 1;

-- Count users
SELECT COUNT(*) FROM users;

-- Example join (future reference)
-- SELECT u.id, u.name FROM users u;

